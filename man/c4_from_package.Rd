% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_package.R
\name{c4_from_package}
\alias{c4_from_package}
\title{Build a C4 diagram from an R package}
\usage{
c4_from_package(
  path = ".",
  level = c("context", "container"),
  include_suggests = TRUE,
  title = NULL
)
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}

\item{level}{Diagram level: \code{"context"} (package as a system surrounded by
its dependencies) or \code{"container"} (each \verb{R/*.R} file as a container).}

\item{include_suggests}{Include packages from \code{Suggests} (rendered with a
dashed line). Default: \code{TRUE}.}

\item{title}{Optional diagram title; defaults to the package name.}
}
\value{
A \code{c4_builder} object. Pipe into \code{\link[=build_context]{build_context()}} or
\code{\link[=build_container]{build_container()}} to render.
}
\description{
Reads \code{DESCRIPTION} (and optionally \verb{R/}) and produces a \code{c4_builder}
representing the package's dependencies (Imports/Depends/Suggests). At the
container level, each \verb{R/*.R} source file is rendered as a container.
}
\examples{
\dontrun{
# Diagram for the package in the current directory
c4_from_package() \%>\% build_context()

# Container-level diagram with R/ files as containers
c4_from_package(level = "container") \%>\% build_container()
}
}

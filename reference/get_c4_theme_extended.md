# Apply theme to diagram (internal helper)

This modifies the helper function to accept custom themes

## Usage

``` r
get_c4_theme_extended(theme)
```

## Arguments

- theme:

  Theme name or c4_theme object

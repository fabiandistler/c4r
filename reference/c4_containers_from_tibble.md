# Create container elements from a tibble or data frame

Create container elements from a tibble or data frame

## Usage

``` r
c4_containers_from_tibble(data)
```

## Arguments

- data:

  A tibble or data frame with columns: id, label, description,
  technology

## Value

A list of container elements

# Build a context diagram from the builder

Build a context diagram from the builder

## Usage

``` r
build_context(builder)
```

## Arguments

- builder:

  A c4_builder object

## Value

A DiagrammeR graph object

# Create component elements from a tibble or data frame

Create component elements from a tibble or data frame

## Usage

``` r
c4_components_from_tibble(data)
```

## Arguments

- data:

  A tibble or data frame with columns: id, label, description,
  technology

## Value

A list of component elements
